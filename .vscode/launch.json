{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Attach to url with files served from ./out",
            "type": "chrome",
            "request": "launch",
            "port": 9222,
            "url": "http://localhost:9222/main.html",
            "webRoot": "${workspaceFolder}/"
        }
    ]
}